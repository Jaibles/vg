<a href="{{ work.url }}">
    <div class="grid grid-cols-6 lg:grid-cols-6 gap-2">
        <div class="col-span-12 lg:col-span-12 relative work-link-activate">
            <div class="work-link transition-all absolute w-full h-full flex justify-center items-center align-center">
                {% if work.data.casestudy %}
                {# <span class="btn btn-secondary py-2">View case study</span> #}
                {% else %}
                <span class="btn btn-secondary py-2">View live ↗</span>
                {% endif %}
            </div>
            <img class="home-work-image rounded-xl" src="{{ work.data.cover }}">
        </div>

        <div class="col-span-12 lg:col-span-12 flex flex-col justify-between mt-3">
            <div>
                <h3 class="text-xl">{{ work.data.client }}</h3>
                <div class="flex flex-col lg:mr-12">
                <span class="text-xl text-gray-600">
                    {% for service in work.data.services %}
                    {{ service }}<span class="comma">,</span> 
                    {% endfor %}
                    </span>
                </div>
            </div>
        </div>

    </div>
</a>